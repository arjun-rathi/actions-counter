---
framework: jest
preRun:
     command:
        - npm ci
preMerge:
  pattern:
    - "./tests/*.js"
postMerge:
  pattern: 
    - "./tests/*.js"
postRun:
     command:
        - node --version
version: 1.0
nodeVersion: 10.24.1
